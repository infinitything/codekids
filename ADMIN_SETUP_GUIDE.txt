🔧 SIMPLE ADMIN DASHBOARD SETUP GUIDE
=======================================

Your CodeKid app now has a SIMPLE admin dashboard!

📍 ADMIN URLS:
--------------
Admin Login:    http://localhost:5173/admin/login
Admin Dashboard: http://localhost:5173/admin

✅ WHAT YOU GET:
----------------
✓ Simple, clean admin dashboard
✓ Platform statistics (users, students, lessons, badges, revenue)
✓ User management (view, edit, delete users)
✓ Lesson management (view, activate/deactivate lessons)
✓ No parent/student dashboards - pure admin interface
✓ Secure admin-only authentication

🚀 SETUP STEPS:
---------------

1. CREATE YOUR DATABASE
   - Run: supabase/migrations/001_initial_schema.sql
   - This creates all tables including 'admins' table

2. CREATE ADMIN ACCOUNT
   Method A - Manual (Recommended):
   a) Sign up a regular account at your app
   b) Go to Supabase Dashboard > SQL Editor
   c) Find your auth user ID:
      SELECT id, email FROM auth.users;
   d) Run:
      INSERT INTO admins (auth_id, email, full_name, permissions)
      VALUES (
        'YOUR_USER_ID_HERE',
        'your@email.com',
        'Your Name',
        ARRAY['manage_users', 'manage_content', 'view_analytics']
      );

   Method B - Quick (see CREATE_ADMIN_USER.sql file)

3. LOGIN AS ADMIN
   - Go to: /admin/login
   - Use your admin email/password
   - You'll be redirected to admin dashboard

4. DEMO MODE (For Testing)
   - Set VITE_DEMO_MODE=true in .env
   - Login with any email/password
   - You'll see demo data

📊 ADMIN DASHBOARD FEATURES:
-----------------------------

OVERVIEW TAB:
- Total Parents count
- Total Students count
- Active Users Today
- Total Lessons available
- Total Badges Earned
- Revenue tracking
- Quick action buttons

USERS TAB:
- View all parent accounts
- See user details (name, email, plan)
- Edit user information
- Delete user accounts
- Filter and search users

LESSONS TAB:
- View all lessons
- See lesson details (title, difficulty, XP)
- Activate/Deactivate lessons
- Edit lesson content
- Add new lessons

🎨 INTERFACE:
-------------
- Clean, modern design
- Easy-to-read tables
- Color-coded status badges
- Responsive layout
- Quick action buttons
- Simple navigation

🔒 SECURITY:
------------
- Admin-only access
- Checks admins table on login
- RLS policies protect data
- Separate login page
- Session management

💡 USAGE TIPS:
--------------
1. Add admin via SQL after signing up
2. Use demo mode for testing
3. Check /admin/login for access
4. All data from real database (no mock data)
5. Toggle lesson status with one click

📁 KEY FILES:
-------------
- SimpleAdminDashboard.tsx - Main admin UI
- AdminLoginPage.tsx - Admin login page
- CREATE_ADMIN_USER.sql - Script to create admin
- 001_initial_schema.sql - Full database setup

🎯 NEXT STEPS:
--------------
1. Run database migration
2. Create your admin account
3. Login at /admin/login
4. Start managing your platform!

Your admin dashboard is ready! 🎉

